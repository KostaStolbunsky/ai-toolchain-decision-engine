# Session note — 2026-05-24

> Recorded at end of session. Use this to resume tomorrow.

---

## What was done today

**Model files created (T-001 through T-004):**
- `model/METAMODEL.md` — core entities (ContextProfile, Node, Implementation, EvaluationFrame, DecisionPath, Recommendation), connection types, versioning rules
- `model/DECISION_TREE.md` — Layer 0 and Layer 1 (solo, department, enterprise routing)
- `model/nodes.md` — 14 abstract node types across 9 lifecycle stages
- `model/use-cases.md` — 10 top-level use case categories
- `model/criteria.md` — 5 primary enterprise criteria with default weights + additional criteria list
- `assessment/ASSESSMENT_SOLO.md` — solo/small team assessment flow
- `assessment/ASSESSMENT_ORG.md` — org/enterprise assessment flow

**Cleanup pass (end of session):**
- Replaced all `quiz` → `assessment` terminology in METAMODEL.md and DECISION_TREE.md
- Removed stale T-002/T-003/T-004 task references from open questions sections
- Updated `docs/open-questions.md`: moved 6 decisions to resolved, restructured remaining open questions by actual status
- STO-30 marked Done in Linear

---

## Where we stopped

Model foundation is complete. Two remaining model gaps that block everything downstream:

| Gap | Task | Blocks |
|---|---|---|
| Tool registry — no schema, no storage format, no example entries | STO-35 | scoring, UI |
| Scoring logic — EvaluationFrame defined but algorithm is not | STO-36 | recommendation output, UI |

UX has not been touched at all. All UX questions remain open.

---

## Next session — suggested starting point

**Option A (recommended):** Start with STO-35 — tool registry schema. Fastest path to real data in the model, which unblocks STO-36 and eventually the UI.

**Option B:** Start with STO-36 — scoring logic. Can be done in parallel with registry.

**Do not start yet:** UX / product design — model gaps above need to close first, otherwise UX decisions will be made in a vacuum.

---

## Open questions to keep in mind

- Should `DecisionPath` steps map 1:1 to assessment questions, or can one step span multiple?
- Should `Recommendation.output_type` be set by the engine or the user?
- EvaluationFrame override conflict resolution: first match, priority order, or union?
- ContextProfile mutability during assessment flow: mutable or locked after first step?
- Department (B) vs Enterprise (C) boundary in Q0: headcount, budget authority, or governance model?

---

## Repo state

- Branch: `main`
- All files committed, no open PRs
- Last commit: `docs: update resolved decisions after T-001 through T-004`
