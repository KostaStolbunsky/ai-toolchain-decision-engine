# Session note — 2026-05-25

> Recorded retroactively on 2026-05-26. Use this to resume after this session.

---

## What was done

**STO-35 — Tool registry schema: Done**
- `model/tool-registry.md` — full field schema for registry entries, storage format, seed set rules, contribution guide
- `registry/schema/` — JSON/YAML schema files
- `registry/` — seed entries with example tools

**STO-36 — Scoring and ranking logic: Done**
- `model/scoring.md` — full 4-phase scoring algorithm with worked example
- Phase 1: hard rules (exclude/require overrides)
- Phase 2: weighted matrix scoring across 5 primary criteria
- Phase 3: boost/penalize overrides applied
- Phase 4: ranking, tie-breaking, confidence calculation
- All type values referenced via `model/types/` registries

**STO-37 — Platform security and threat model: Done**
- `docs/security/threat-model.md` — full threat model: attack surfaces, actors, risk levels, mitigations
- Threats covered: registry poisoning, user submission abuse, agent manipulation, audit trail integrity
- Source credibility scoring model kept private (not published)

**ADR-005 — Data storage architecture: Accepted**
- `adr/adr-005-data-storage-architecture.md` — storage decisions for tool registry, user sessions, decision paths
- Open questions from ADR captured as STO-47, STO-48, STO-49

**Open questions sweep (end of session):**
- All open questions from METAMODEL, DECISION_TREE, ASSESSMENT_SOLO, ASSESSMENT_ORG, ADR-005 exported to Linear as STO-39 through STO-58
- You manually added `open-question` label and linked to project in Linear (I forgot to do this — fixed)

---

## Where we stopped

The full model foundation is complete:

| Layer | Status |
|---|---|
| Meta-model (METAMODEL.md) | Done — STO-30 |
| Decision tree (DECISION_TREE.md) | Done — STO-30 |
| Node taxonomy (nodes.md) | Done — STO-30 |
| Use case taxonomy (use-cases.md) | Done — STO-30 |
| Criteria schema (criteria.md) | Done — STO-30 |
| Assessment flows (SOLO + ORG) | Done — STO-30 |
| Type registries (model/types/) | Done — STO-30 |
| Tool registry schema | Done — STO-35 |
| Scoring algorithm | Done — STO-36 |
| Security / threat model | Done — STO-37 |
| Infrastructure design | Backlog — STO-38 |

---

## Next session — suggested starting point

Model is complete. Ready to move to the next phase. Two options:

**Option A (recommended):** Define the implementation architecture — what gets built, in what stack, in what order. Feed into STO-38 (infrastructure design).

**Option B:** Seed the tool registry with real entries for the most common node types (LLM provider, orchestration, evaluation). Validates the schema against real data before build starts.

**Do not start yet:** UI/frontend — needs implementation architecture decision first.

---

## Open questions in backlog (STO-39 through STO-58)

All tagged `open-question`, all linked to project. Do not resolve until the relevant implementation phase is ready.
